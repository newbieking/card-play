{
  "name": "card-play",
  "version": "0.1.0",
  "private": true,
  "description": "卡牌冒险养成游戏 Monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:watch": "pnpm --filter @cardplay/battle-engine test:watch",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
