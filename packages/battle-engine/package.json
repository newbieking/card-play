{
  "name": "@cardplay/battle-engine",
  "version": "0.1.0",
  "description": "卡牌游戏确定性战斗引擎",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cardplay/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "typescript": "^5.3.0"
  }
}
