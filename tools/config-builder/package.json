{
  "name": "@cardplay/config-builder",
  "version": "0.1.0",
  "description": "配置表构建工具 - CSV → JSON + TS 类型",
  "private": true,
  "scripts": {
    "build": "ts-node src/index.ts",
    "generate": "ts-node src/index.ts --output ../shared/src/config"
  },
  "dependencies": {
    "csv-parse": "^5.5.0",
    "glob": "^10.3.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
